/*
 * CONFIG: Rapid Prototyping (CD/DVD version)
 * Goal: Fast iteration cycles with cheap media. Lower performance but quick to test ideas.
 */

disc_od = 120;
disc_thickness = 1.2;
num_discs = 12;            // Much shorter stack for fast prints
gap = 1.4;                 // Forgiving gap

nozzle_count = 6;
nozzle_angle = 11;
nozzle_w = 6.0;
nozzle_h = 2.0;

hv_dia = 34;               // Slightly oversized for easy fitting with real CDs
diffuser_len = 35;
diffuser_outlet_dia = 46;
